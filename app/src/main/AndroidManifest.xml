<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.robert.test4">

    <application
        android:name=".CompareList"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SActivity"
            android:label="Smart Search"></activity>
        <activity
            android:name=".QueryActivity"
            android:background="#44441a"
            android:label="@string/app_name"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".SmartSearchListActivity"
            android:label="Results" />
        <activity
            android:name=".displaytest"
            android:label="@string/title_activity_displaytest"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SMSListActivity"
            android:label="@string/title_activity_smslist"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SearchPage" />
        <activity android:name=".DisplayPage" />
        <activity android:name=".FiltersForSearch" />
        <activity android:name=".ComparePage" />
        <activity android:name=".MainActivity"
            android:label="Search">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FilterSearchResults"
            android:label="Results"/>
        <activity android:name=".CompareTwo"></activity>
    </application>

</manifest>